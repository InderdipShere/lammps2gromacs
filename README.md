Lammps to Gromacs toolkits

MOF can be difined by UFF (and its derevatives) using lammps-interface. However, if we need export to Gromacs, we can still used data.system file to generate the gromacs input. 
